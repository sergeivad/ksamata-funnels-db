/**
 * Константы фазы 15, отдельным файлом по общему укладу фаз: и сама фаза, и
 * тесты берут их отсюда, а не повторяют строки у себя.
 */
import { PHASE14_STAGE_TAG } from './migrate-phase14-data';

/**
 * Старое написание тега этапа — «Предписок», без «с».
 *
 * Литерал, а не производная от чего-либо: это то, что лежит в существующих
 * базах, и меняться оно уже не может. Значение заморожено здесь навсегда,
 * иначе фаза перестанет узнавать то, что переписывает.
 */
export const PHASE15_OLD_STAGE_TAG = 'АВ Этап: Предписок';

/**
 * Новое написание — берётся у фазы 14, а не повторяется строкой.
 *
 * Так две стороны не разъедутся: шаблон свежей базы засевает фаза 14 (через
 * PHASE5_TEMPLATE_SEED), существующую переписывает фаза 15, и обе обязаны
 * писать одно и то же.
 */
export const PHASE15_NEW_STAGE_TAG = PHASE14_STAGE_TAG;
