/**
 * Переименование вида блока фазы 13, отдельным файлом по общему укладу фаз:
 * и сама фаза, и тест берут пару слагов отсюда, а не повторяют строки у себя.
 */

/** Слаг, каким блок «Допродажи / дожим» назывался до фазы 13. */
export const PHASE13_OLD_KIND = 'meditation';

/** Слаг после неё. */
export const PHASE13_NEW_KIND = 'upsell';
