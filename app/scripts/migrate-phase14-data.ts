/**
 * Константы фазы 14, отдельным файлом по общему укладу фаз: и сама фаза, и
 * тесты берут их отсюда, а не повторяют строки у себя.
 */

/** Пятый сценарий тегов. Совпадает со значением Scenario в src/lib/ab-tags.ts. */
export const PHASE14_SCENARIO = 'predspisok';

/**
 * Тег этапа, который кладётся в шаблон.
 *
 * Написание — «Предсписок», через «с». Правило прежнее и единственное: тег
 * повторяет живой реестр предложений GetCourse дословно, потому что наборы
 * сравниваются с ним посимвольно. Сменилось не правило, а реестр.
 *
 * До августа 2026 этап там писался «Предписок», без «с» — опечатка GetCourse,
 * которую мы обязаны были повторять. Между 30.07 и 01.09 GetCourse её
 * исправил: в снимке 30.07 старое написание несли 14 предложений и ни одного
 * нового, в снимке 01.09 старого нет вовсе, а новое несут 34 предложения —
 * те же 14 живы, у них просто сменился тег. Старая константа после этого
 * давала честный ноль совпадений и выглядела как «расхождений нет».
 *
 * Та же строка с тем же доводом — PREDPISOK_STAGE в tools/audit/normalize.py.
 * Продублирована, а не импортирована, по очевидной причине: языки разные.
 * При правке — править обе стороны.
 *
 * Существующие базы переписывает фаза 15: сид этой строки разовый, за
 * маркером, и одной сменой константы шаблон не поправить.
 */
export const PHASE14_STAGE_TAG = 'АВ Этап: Предсписок';

/** Маркер в schema_migrations, гасящий повторный сид строки шаблона. */
export const PHASE14_SEED_MARKER = 'phase14_predspisok_seed';

/**
 * Таблицы, у которых CHECK перечисляет сценарии, и колонка, которую он
 * ограничивает. SQLite не умеет ALTER для CHECK, поэтому каждую придётся
 * перестроить целиком (см. migrate-phase14.ts).
 */
export const PHASE14_SCENARIO_TABLES: { table: string; column: string }[] = [
  { table: 'funnel_tags', column: 'tag_type' },
  { table: 'tag_templates', column: 'scenario' },
  { table: 'funnel_tag_overrides', column: 'tag_type' },
];
