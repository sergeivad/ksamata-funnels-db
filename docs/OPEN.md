# Открытые вопросы — единый список

> ⚠️ **Этот файл ведётся руками и потому устаревает.** С 2026-08-04 рабочий
> список даёт инструмент: `python3 tools/reconcile/run.py` → отчёт в
> `data/generated/`. Порядок разбора —
> [2026-08-04-razbor-design.md](plans/2026-08-04-razbor-design.md), **начинать
> сессию с него.** Здесь остаётся то, что инструмент пока не покрывает:
> вопросы к ЛИК, схема, безопасность.
>
> Уже разошлось с реальностью: пункт 1 (ВК NR «ЖИВО Суставы 490р») инструмент
> показывает сам; пункт 5 закрыт 04.08 — из семи расхождений по статусам
> осталось одно. Пункт 20 (93 заказа без осей) занижен на два порядка —
> их 26 133 оплаченных.

Актуально на **2026-08-04**. Это **указатель, а не источник истины**: по каждому
пункту одна строка и ссылка на план, где он разобран подробно. Закрывая пункт,
правьте и план, и эту строку.

Сгруппировано по тому, **кто должен действовать** — потому что половина
открытого чинится не в этом репозитории.

---

## С чего начинать следующую сессию

1. **ВК NR «ЖИВО Суставы 490р»** — воронка работает с 01.08 и копит заявки, а
   её нет ни в базе, ни в ЛИК. Единственный пункт, где идёт потеря данных прямо
   сейчас. → [table-sync](plans/2026-08-04-table-sync.md)
2. **Три неизвестных, блокирующих любые правки в ЛИК** (пункты 14-19 ниже
   заморожены до ответа). Цена ошибки — пересчёт заявок неизвестной глубины.
   → [tag-filter-audit](plans/2026-08-04-leak-tag-filter-audit.md)
3. **19 заказов считаются дважды** — ЛИК засчитывает ЖКТ-квизовые заказы и в
   `f8`, и в `f43`, потому что воронки «ЖКТ-квиз» нет ни в базе, ни в ЛИК.
   → [tag-filter-audit](plans/2026-08-04-leak-tag-filter-audit.md)
4. **Развилка «что считать шагом воронки»** — блокирует классы 1 и 5 карты
   расхождений и воронку `f42`. Начинать с brainstorm, не с правки.
   → [tag-drift-triage](plans/2026-07-25-tag-drift-triage.md)

> ✅ **База и прод сошлись 2026-08-04.** Десять комнат `f84` перенесены на обе
> стороны, `funnel_days` = 518 и там, и там; сами десять строк сверены дословно
> через `/api/funnels/78/days`. Бэкап прода до записи —
> `/data/backup_20260804_f84rooms.db`.

---

## Нам — данные в базе

| # | Пункт | Где разобрано |
|---|---|---|
| 1 | 🔴 **ВК NR «ЖИВО Суставы 490р»** — работает с 01.08, в базе нет. Недостающий четвёртый элемент набора ВК NR (`f73`, `f74`, `f78` есть, ЖИВО-суставы нет; у ИНХАУЗ набор полный). В ЛИК тоже нет: нужен код оттуда либо заведение без кода | [table-sync](plans/2026-08-04-table-sync.md) |
| 2 | **`f43` — статус.** У нас `archive`, в ЛИК `ACTIVE`. Оси совпадают буква в букву | [table-sync](plans/2026-08-04-table-sync.md) |
| 3 | **Заводить ли «ЖКТ / НИМБ / Яндекс / РСЯ + АВ Квиз»** — 19 оплаченных заказов сейчас принадлежат `f8` и `f43` одновременно | [tag-filter-audit](plans/2026-08-04-leak-tag-filter-audit.md), п. 8 |
| 4 | **Комнаты дней 4-5** для `f8`, `f9`, `f12`, `f13` и безкодовой `#14` — таблица владельца их даёт (20 строк, продолжают паттерн), в базе только дни 1-3 | [table-sync](plans/2026-08-04-table-sync.md) |
| 5 | ✅ **Расхождения по статусам — разобрано 04.08.** Из семи осталось одно: `f54` (заведена 12 дней назад, заказы её рассудить ещё не могут). `f27` → `archive`; по `f8`, `f9`, `f31`, `f29`, `f28` заказы подтвердили базу — правится таблица. «Пауза владельца 02.08» касалась спора базы с **ЛИК**, а не с таблицей, и была им же снята | [razbor-design](plans/2026-08-04-razbor-design.md) |
| 6 | **Второй лист таблицы «НЕ заполняю, но висят»** — 8 зарубежных воронок (`.com`-домены, комнаты `-ym`/`-yt`), ни одной в базе. Источника «Ютуб Траф» тоже нет | [table-sync](plans/2026-08-04-table-sync.md) |
| 7 | **Дашборды и сегменты ГК** из таблицы не переносили ни одного: у многих строк это ссылка на редактор воронки (`funnel/update?id=`), а не дашборд | [table-sync](plans/2026-08-04-table-sync.md) |
| 8 | **3 недоступных ленда** по мониторингу — список снят до правки `f28` и устарел (`vkb/boo/a` больше не её адрес). Перепроверить | досье basic-memory |

## Нам — схема и код

| # | Пункт | Где разобрано |
|---|---|---|
| 9 | **Развилка: что считать шагом (сценарием) воронки.** База знает четыре (`reg / time_15 / time_19 / messenger`), реальность не влезает: у перелива нет своего шага регистрации, новые воронки не размечают временем, появился этап «Предписок». Пока не решено — классы 1 и 5 не разбираются | [tag-drift-triage](plans/2026-07-25-tag-drift-triage.md) |
| 10 | **`f42` заблокирована схемой** — у неё не четыре времени, а два времени × два дня старта; не хватает измерения «поток». Уходит в пункт 9 | [leak-sync](plans/2026-08-02-leak-sync.md), п. 3 |
| 11 | **`room_id_f1`** — убрать мёртвую колонку или оставить. Ничего её не читает, `replaceDays` обнуляет её на каждой правке сетки | [leak-sync](plans/2026-08-02-leak-sync.md), п. 1 |
| 12 | **Инструмент аудита, две правки:** класс 14 не смотрит на дату создания предложения (свежая линейка выглядит «кандидатами в архив»); классам 5/2/7 нужен порог «широты» выгрузки, чтобы отделить историю от живого | досье basic-memory |
| 13 | **UX-бэклог** — открытые пункты: DRY-рефакторинги, N+1 в `listFunnels`, легаси-колонки схемы, отставка старых целей мониторинга | [ux-improvements-backlog](plans/2026-07-18-ux-improvements-backlog.md) |

## 🚧 Неизвестные, блокирующие правки в ЛИК

Это **не задачи, а вопросы без ответа**. Пока ответа нет, пункты 14-19 трогать
нельзя: цена ошибки — пересчёт заявок неизвестной глубины.
Разобрано в [tag-filter-audit](plans/2026-08-04-leak-tag-filter-audit.md).

| # | Неизвестное | Кому |
|---|---|---|
| A | **Что значит `effectiveFrom = null` у существующего набора правил** — вся история или «не пересчитывать»? Замерено: `null` стоит у **43 наборов из 60**, включая `f43` и `f8`, которые и собираемся править. Правка любой из них — ещё и переход из режима «без даты» в режим «с датой» | besales |
| B | **Есть ли откат активации** — переактивация архивного набора, отмена пересчёта. В клиентском бандле ручки не нашлось | besales |
| C | **Исключает ли `INACTIVE` воронку из атрибуции.** От этого зависит весь порядок работ: если да, двойной счёт 19 заказов у `f43` снимается простым выключением — без правки правил, без даты, без пересчёта, с тривиальным откатом. Проверяется на `f16` — единственной `INACTIVE` в реестре | проверить самим |

## Владельцу — в LeakEngine

⚠️ Всё ниже **заморожено до ответов на A-C.** Писать в ЛИК только по прямой
просьбе владельца: активация набора правил запускает пересчёт заявок.
См. [leak-engine.md](leak-engine.md).

| # | Пункт | Где разобрано |
|---|---|---|
| 14 | **Переписать `required_offer_tags` на АВ-набор** у восьми воронок с легаси-фильтром — иначе выборки ЛИК и базы продолжат расходиться | [tag-filter-audit](plans/2026-08-04-leak-tag-filter-audit.md), п. 1 |
| 15 | **И у пятнадцати «совпавших по случайности»** (`f6`, `f9`, `f11`, `f12`, `f13`, `f15`, `f16`, `f19`, `f21`, `f23`, `f24`, `f25`, `f36`, `f38`, `f40`) — сегодня сходятся, но держатся на данных, а не на настройке | [tag-filter-audit](plans/2026-08-04-leak-tag-filter-audit.md), п. 2 |
| 16 | **`f43`** — добавить в фильтр ось `АВ Продукт: ЖИВО` и заменить `квиз`. Без оси продукта «ЖИВО-квизы» ловят ЖКТ-квиз | [tag-filter-audit](plans/2026-08-04-leak-tag-filter-audit.md), п. 3 |
| ~~17~~ | ✅ **`f84` — сделано 04.08.** Владелец дописал `dbo4-19-kvch` десятой; в реестре снова десять кодов, сверено. В базу и на прод перенесены все десять | [tag-filter-audit](plans/2026-08-04-leak-tag-filter-audit.md), п. 4 |
| 18 | **Выключить семь воронок** — в базе `archive`, в ЛИК числятся живыми: `f6`, `f12`, `f13`, `f19`, `f33`, `f43`, `f58` | [leak-todo](plans/2026-08-02-leak-todo.md), п. 2 |
| 19 | **`f83` — имя:** «ДБО / Внутренний / Перелив / Со всех» → «С БОО» | [leak-todo](plans/2026-08-02-leak-todo.md), п. 1 |

## Владельцу — в GetCourse

| # | Пункт | Где разобрано |
|---|---|---|
| 20 | **93 оплаченных заказа `ЖИВО` без осей** — разметка | [tag-filter-audit](plans/2026-08-04-leak-tag-filter-audit.md), п. 9 |
| 21 | **«Предсписок ЖКТ РСЯ»** без оси продукта | досье basic-memory |
| 22 | **4 осиротевших тега на проде** — через API не убрать (`PATCH`/`DELETE /api/refs/tags/*` запрещены намеренно), чистится скриптом по ssh | досье basic-memory |

## Владельцу — в таблице «Ссылки для сбора статы»

Иначе следующая выгрузка принесёт то же самое.

| # | Пункт | Где разобрано |
|---|---|---|
| 23 | **`f25` / `f26` — комнаты устарели.** Верно: `cvc1-15-yanr`…`cvc5-19-yanr` и `dbo1-15-yanr`…`dbo5-19-yanr`. У `f26` третий день продублирован на 4-й и 5-й | [table-sync](plans/2026-08-04-table-sync.md) |
| 24 | **`f30` — две опечатки в комнатах:** день 1/19:00 записан `dih51-19-faq`, день 2/19:00 — `dih3-19-faq`. Верно: `dih1-19-faq`, `dih2-19-faq` | [table-sync](plans/2026-08-04-table-sync.md) |
| 25 | **Строка 23 — код `F46` вместо `f47`** (дубль с строкой 60) | [table-sync](plans/2026-08-04-table-sync.md) |

## Безопасность

| # | Пункт |
|---|---|
| 26 | 🔴 **Ротировать Dokploy API-токен** (`panel.ksamata.ru`) — засвечен в переписке многократно, экспозиция накопительная. Единственный незакрытый пункт по безопасности после закрытия прода авторизацией 31.07 |

---

## Что решено и НЕ надо переоткрывать

- **Квизы не заводим.** Решение 29.07, подтверждено 30.07 замером и повторно
  04.08 на другом наборе: 6 квизов + 2 прямые продажи ЖКТ из таблицы — все
  «Стоп», в ЛИК их нет, карточки вышли бы пустыми. Отсутствуют шесть из восьми:
  строки 18 и 20 покрыты существующей `f43`.
- **`f28` — одна воронка, а не две.** Её передали другому подрядчику; состав
  прежний, AV-теги добавлены сверху, legacy оставлены. Вторую заводить не надо,
  дашборды остаются при ней.
- **Класс 15 не трогать** — он сторож, а не прибор.
- **Мёртвые связки в базу не заводить**, даже как `archive`: знание об
  отставленных живёт в `tools/audit/retired.py`.
- ⚠️ **Таблица владельца по комнатам — не эталон**: она копирует мёртвую
  `room_id_f1`. При расхождении по комнатам правится таблица, а не база.
