# Funnels Admin — Карточка воронки: блоки ссылок — дизайн (черновик)

**Дата:** 2026-06-25
**Статус:** ЧЕРНОВИК (шаблон v8 одобрен визуально; данные/миграцию подтвердить)
**Репозиторий:** `ksamata-funnels-db`, приложение в `app/` (продолжение Фаз 1–2)
**Утверждённый макет:** `funnel_card_with_identity_v8` (визуальный черновик в сессии)

## 1. Зачем

Сейчас карточка воронки показывает идентичность отдельной формой сверху и
сетку «5 дней × 2 времени» (`DaysEditor`) только с тремя полями (GC-комната,
Web-комната, ленд). При этом в `funnel_days` уже лежит 17 колонок и 300
заполненных строк (тарифы, ОТО, бонусы, процессы, повторы, медитации) —
невидимых в UI. Источник данных — `Воронки ссылки-5.xlsx` (лист на продукт,
9 типов ссылок по дням × времени).

Цель: пересобрать карточку в **одну цельную форму** — идентификация сверху,
ниже — тематические блоки ссылок, — и сделать доступными для редактирования
все типы ссылок, разложенные по понятным блокам.

## 2. Структура карточки (сверху вниз)

### 2.1. Идентификация
- **Код `f#`** — редактируемое поле-плашка слева сверху (`funnels.front_code`).
- **Имя воронки** — заголовок, **только для чтения**, собирается из осей:
  `«{Продукт} / {Подрядчик} / {Канал} / {Направление}»`
  (напр. `БОО / NR / ВК / Перелив с БОО`). Отдельного поля «Вариант/название» нет.
- **Статус** — сегмент Активна / Черновик (`funnels.status`).
- **Оси (селекты с добавлением нового):** Продукт, Подрядчик, Канал, Направление.
  Источники опций — `products`, `contractors`, `channels`, `directions`
  (как в Фазе 2). АВ-теги генерируются из осей при сохранении.
- **Комментарий** — свободный многострочный текст (новая колонка
  `funnels.comment`).
- **АВ-теги** — read-only плашка-список: `Продукт: X`, `Подрядчик: X`,
  `Канал: X`, `Направление: X`, `Автоворонка`, `Этап: Регистрация/Оплата`,
  `Время: A / B`. Генерируются автоматически (логика `axesToTagNames`), руками
  не редактируются.
- **Убрано из шапки:** поле «Вариант/название», «Дата старта», «Лендинг»
  (лендинги переехали в блок), «Блок» (уже убрано в Фазе 2). Колонки
  `funnels.variant`, `start_date`, `landing_url` остаются в БД, но не
  редактируются (`landing_url` мигрирует в блок «Лендинги», см. §5).

### 2.2. Время воронки
- Два редактируемых поля, по умолчанию **15:00** и **19:00**, но значения
  можно изменить на любые. Хранятся в новых колонках `funnels.time_label_a`
  (деф. `15:00`) и `time_label_b` (деф. `19:00`).
- **Внутренние ключи слотов неизменны:** `A = '15'`, `B = '19'` (это
  идентификаторы, не отображаемый текст). Они используются в `funnel_days` и в
  `funnel_block_items.slot`. Меняется только подпись (`time_label_*`). Так не
  трогаем `CHECK(time_slot IN ('19','15'))` и существующие 300 строк.
- Порядок везде: сначала слот A (15), затем слот B (19).

### 2.3. Блоки (порядок)
1. **Вебинарные комнаты** — особый блок (см. §3).
2. **Лендинги** — список, одно поле, всегда «Общее».
3. **Страницы тарифов** — список, одно поле.
4. **Оформление заявки** — список, одно поле.
5. **Бонусы** — список, одно поле.
6. **Процессы** — список, два поля (описание + ссылка).
7. **ОТО** — список, одно поле.
8. **Медитация / дожим** — список, два поля.
9. **Ссылки / дашборды** — список, два поля (существующий гибкий блок).

## 3. Блок «Вебинарные комнаты» (особый)

- Всегда активен (нельзя выключить), всегда показывает **оба времени**
  колонками: слот A (15:00) и слот B (19:00), рядом.
- Каждый слот — 5 строк (дни 1–5). Поля на ячейку: **GC-комната**,
  **Web-комната**, и опционально **Повтор**.
- **Повтор** — тумблер в шапке блока (`funnels.rooms_replay_enabled`):
  выкл → колонки повтора нет; вкл → появляется третья колонка (повтор).
- Хранение: остаётся таблица `funnel_days`, используется подмножество колонок:
  `gc_room`, `web_room`, `replay_url` (повтор). Прочие колонки `funnel_days`
  (sales_page, tariffs, oto, …) после миграции (§5) не редактируются и
  считаются устаревшими.
- Плейсхолдеры в пустых полях.

## 4. Блоки-списки (единый компонент)

Все блоки кроме Комнат — один переиспользуемый компонент, параметризованный
**видом блока** (`kind`). Поведение:

- **Тумблер вкл/выкл** — активация блока для воронки (`funnel_blocks.enabled`).
  Выключенный блок свёрнут и приглушён.
- **Режим** (`funnel_blocks.mode`): `common` («Общее») или `by_time`
  («По времени»). Сегмент-переключатель в шапке.
  - `common` → один список строк.
  - `by_time` → две подколонки, подписанные `time_label_a` / `time_label_b`,
    каждая со своим списком строк.
  - Исключение: **Лендинги** — всегда `common`, переключатель режима скрыт.
- **Поля строки** — свойство вида блока (не хранится в БД, задано в коде):
  - **одно поле** (только ссылка): Лендинги, Страницы тарифов, Оформление
    заявки, Бонусы, ОТО.
  - **два поля** (описание + ссылка): Процессы, Медитация/дожим,
    Ссылки/дашборды.
- Действия: добавить строку, удалить строку, переупорядочить (`position`).
- Ссылка рендерится кликабельной только при `http(s)://` (гард `isSafeUrl`).
- Плейсхолдеры во всех пустых полях; тёмный цвет плейсхолдера (`#8A8275`).

**Каталог видов блоков** (константа в `app/src/lib/blocks.ts`):

| kind | Заголовок | Иконка | Полей | Режимы | По умолчанию |
|------|-----------|--------|-------|--------|--------------|
| `landings`     | Лендинги           | browser             | 1 | common | вкл |
| `tariffs`      | Страницы тарифов   | discount            | 1 | common, by_time | вкл |
| `applications` | Оформление заявки  | file-invoice        | 1 | common, by_time | вкл |
| `bonuses`      | Бонусы             | gift                | 1 | common, by_time | выкл |
| `processes`    | Процессы           | settings-automation | 2 | common, by_time | выкл |
| `oto`          | ОТО                | flame               | 1 | common, by_time | выкл |
| `meditation`   | Медитация / дожим  | yoga                | 2 | common, by_time | выкл |
| `links`        | Ссылки / дашборды  | link                | 2 | common, by_time | вкл |

(Поле «по умолчанию вкл/выкл» применяется только при создании НОВОЙ воронки;
для существующих — определяется миграцией, §5.)

## 5. Схема БД и миграция

### 5.1. Изменения схемы (идемпотентная миграция)

```sql
-- Идентификация / время / повтор
ALTER TABLE funnels ADD COLUMN comment              TEXT    DEFAULT '';
ALTER TABLE funnels ADD COLUMN time_label_a         TEXT    DEFAULT '15:00';
ALTER TABLE funnels ADD COLUMN time_label_b         TEXT    DEFAULT '19:00';
ALTER TABLE funnels ADD COLUMN rooms_replay_enabled INTEGER DEFAULT 0;

-- Конфиг блоков на воронку
CREATE TABLE IF NOT EXISTS funnel_blocks (
  id        INTEGER PRIMARY KEY AUTOINCREMENT,
  funnel_id INTEGER NOT NULL REFERENCES funnels(id) ON DELETE CASCADE,
  kind      TEXT    NOT NULL,
  enabled   INTEGER NOT NULL DEFAULT 0,
  mode      TEXT    NOT NULL DEFAULT 'common' CHECK(mode IN ('common','by_time')),
  UNIQUE(funnel_id, kind)
);

-- Строки блоков
CREATE TABLE IF NOT EXISTS funnel_block_items (
  id       INTEGER PRIMARY KEY AUTOINCREMENT,
  block_id INTEGER NOT NULL REFERENCES funnel_blocks(id) ON DELETE CASCADE,
  slot     TEXT    DEFAULT NULL CHECK(slot IN ('15','19') OR slot IS NULL),
  label    TEXT    NOT NULL DEFAULT '',
  url      TEXT    NOT NULL DEFAULT '',
  position INTEGER NOT NULL DEFAULT 0
);
CREATE INDEX IF NOT EXISTS idx_fbi_block ON funnel_block_items(block_id);
```

- `slot` = NULL для строк блока в режиме `common`; `'15'`/`'19'` для `by_time`.
- Существующая таблица `funnel_links` (0 строк) **поглощается** новой моделью
  как `kind='links'` и далее не используется (можно оставить пустой).
- `funnel_days` сохраняет строки/колонки; UI редактирует только
  `gc_room`/`web_room`/`replay_url`. Остальные колонки — устаревшие.

### 5.2. Перенос существующих данных (best-effort, по каждой воронке)

Источник — `funnel_days` (300 строк) + некоторые колонки `funnels`. Для каждой
воронки создаём `funnel_blocks` (enabled=1, если найдены непустые данные) и
наполняем `funnel_block_items`:

| Источник | → Блок (`kind`) | slot | label | url |
|----------|------------------|------|-------|-----|
| `funnels.landing_url` | `landings` | NULL (common) | '' | landing_url |
| `funnel_days.sales_page` | `applications` | time_slot | '' | sales_page |
| `funnel_days.tariffs` | `tariffs` | time_slot | '' | tariffs |
| `funnel_days.oto` | `oto` | time_slot | '' | oto |
| `funnel_days.bonuses` | `bonuses` | time_slot | '' | bonuses |
| `funnel_days.mission` | `processes` | time_slot | `mission_type` (или `«День N»`) | mission |
| `funnel_days.meditation` | `meditation` | time_slot | `dojim_note` | meditation |
| `funnels.dash_*`, `regi_*_url`, `predspisok_url` (непустые) | `links` | NULL | имя колонки | URL |

- **Режим блока после миграции:** если у воронки непустые значения есть только в
  одном слоте → `common` (slot строк → NULL); если в обоих слотах → `by_time`.
- **`replay_url`/`web_replay`** остаются в комнатах; `rooms_replay_enabled=1`,
  если у воронки есть непустой повтор.
- **Спорные/мусорные значения** (нужен ручной разбор, мигрируются как есть с
  пометкой в `label`, либо логируются отчётом):
  - `sales_note`/`dojim_note` содержат и заметки («тарифы с записью», «дожим в
    ГК по куп больш курс»), и иногда полноценные URL — переносятся в
    `tariffs`/`meditation` как доп. строки, текст-заметка идёт в `label`.
  - `web_room`/`web_replay` дублируют GC-вариант (room/…) — это нормально,
    остаются в комнатах.
- Миграция **идемпотентна** и **не трогает** уже заполненные вручную
  `funnel_blocks`/`funnel_block_items` (выполняется один раз; повторный запуск —
  no-op при наличии блоков).
- **Бэкап БД перед миграцией** (как в Фазе 2).

## 6. API

- `GET /api/funnels/[id]` → идентичность + производное имя + АВ-теги +
  `time_label_a/b` + `rooms_replay_enabled` + список блоков с их items.
- `PUT /api/funnels/[id]` → идентичность: `frontCode`, `status`, оси
  (product/contractor/channel/direction), `comment`, `time_label_a/b`,
  `rooms_replay_enabled`. Авто-источник и АВ-теги пересобираются как в Фазе 2.
- `GET/PUT /api/funnels/[id]/rooms` → сетка 2 слота × 5 дней
  (`gc_room`, `web_room`, `replay_url`); reconcile поверх `funnel_days`
  (UPDATE только трёх колонок, прочие сохраняются).
- `GET/PUT /api/funnels/[id]/blocks/[kind]` → `{ enabled, mode, items }`;
  PUT в одной транзакции: upsert конфига блока + replace всех строк
  (delete-all + insert, `position` = индекс, `slot` по режиму).
- `/api/refs/[kind]` — без изменений (products/contractors/channels/directions).

## 7. Компоненты (UI)

- `FunnelIdentity` — шапка: редактируемый код-плашка, производное имя (read-only),
  статус-сегмент, селекты осей (RefSelect с добавлением), комментарий,
  read-only АВ-теги.
- `TimeLabels` — два редактируемых поля времени.
- `RoomsEditor` — 2 слота × 5 дней, GC/Web/(повтор), тумблер повтора.
- `BlockEditor` — generic по `kind`: шапка (тумблер вкл/выкл, сегмент режима
  если режимов >1), список строк (1 или 2 поля), добавить/удалить/сортировать;
  `common` → один список, `by_time` → две подколонки по `time_label_*`.
- `app/src/app/funnels/[id]/page.tsx` — компонует: `FunnelIdentity` →
  `TimeLabels` → `RoomsEditor` → перечисление `BlockEditor` по каталогу видов.
- Список воронок (`app/src/app/page.tsx`) — имя в формате
  `«Продукт / Подрядчик / Канал / Направление»`.

## 8. Дизайн / стиль

Кремовый «ксаматовский» стиль (токены `app/src/lib/tokens.css`): тёплый фон,
`#111` акценты, оранжевый `#FF6A13` для активных тумблеров/действий, моно-чипы
кодов и ссылок, мягкие карточки. Тёмные плейсхолдеры (`#8A8275`). Без
поясняющих подписей-«лесов» в живом UI.

## 9. Что НЕ меняем

АВ-логика/таксономия тегов; существующие колонки `funnel_days` (после миграции
устаревшие, не удаляются); деплой (Docker/Dokploy) — новые таблицы создаются
идемпотентной миграцией, в т.ч. при первом старте на volume, как в Фазе 2.

## 10. Открытые вопросы (подтвердить перед планом)

1. **Миграция данных** — переносим существующие 300 строк по таблице §5.2, или
   стартуем блоки пустыми и заполняем вручную? (Рекомендую перенос.)
2. **Спорные `sales_note`/`dojim_note`** — устроит ли best-effort перенос с
   текстом-заметкой в `label`, или эти значения разобрать вручную отдельно?
3. **Порядок блоков** §2.3 — ок, или поднять/опустить какие-то?
4. **Дашборд-колонки** (`dash_*`, `regi_*`, `predspisok_url`) — переносить в
   блок «Ссылки/дашборды» (рекомендую) или оставить вне карточки?

## 11. Тестирование

- Юнит (temp-копия БД): миграция схемы + перенос данных (раскладка по блокам,
  определение режима common/by_time, повтор-флаг); `funnel-blocks` reconcile
  (upsert конфига + replace строк, slot по режиму); производное имя из осей;
  АВ-теги из осей; rooms reconcile (сохранность прочих колонок).
- Сборка и полный прогон Vitest зелёные; ручная проверка карточки: шапка,
  комнаты с/без повтора, блок в `common` и `by_time`, вкл/выкл блока, `/refs`.

## 12. Критерии готовности

- Карточка = одна форма: идентичность (код в плашке, имя из осей, оси-селекты,
  комментарий, авто-теги) + время (деф. 15/19, редактируемое) + блоки.
- Комнаты всегда оба времени (15→19), повтор тумблером.
- Блоки-списки: вкл/выкл, режим Общее/По времени (Лендинги — всегда Общее),
  одно/два поля по каталогу; добавление/удаление/сортировка; плейсхолдеры.
- Существующие данные перенесены в новую модель миграцией; деплой не сломан.
- Тесты и сборка зелёные.
