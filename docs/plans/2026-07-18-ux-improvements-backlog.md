# Бэклог доработок админки (UX-ревью 2026-07-18)

Итоги прохода по UI (список → карточка → справочники) + код-ревью.
Общий вывод: база добротная, карточка воронки удобная; ниже — что докрутить.
Приоритеты: P1 — мешает каждый день, P2 — скоро начнёт мешать, P3 — по случаю.

Обновление 2026-07-18 (вечер): P1/P2 реализованы четырьмя агентами
(поиск+фильтр, fail-closed auth, CRUD справочников, dirty-гард), пройдено
мультиагентное код-ревью (8 углов), 2 найденных бага исправлены
(immutable tags kind, product_durations → 409 вместо 500). 197/197 тестов.

## P1. Быстрый переход по ссылкам — СДЕЛАНО

- [x] `UrlInput`: иконка «открыть в новой вкладке» рядом с копированием, на hover.
- [x] `BlockListField` (лендинги, тарифы, бонусы, ОТО…): колонка ExternalLink;
      `RoomsEditor` покрыт через `UrlInput`.

## P1. Список воронок — СДЕЛАНО

- [x] Заголовок и шеврон карточки — настоящие `<Link>`, работает Cmd+клик.
- [x] Поиск по списку (имя, frontCode, «f5» и «5» находят f5), очистка X/Esc,
      счётчик «X из N», пустые группы скрываются.
- [x] Фильтр по статусу «Все / Активные / Черновики» (localStorage).
- [x] После «Дублировать» — переход на карточку новой воронки.

## P1. Авторизация fail-closed — СДЕЛАНО

- [x] `middleware.ts`: в production без валидной `ADMIN_BASIC_AUTH` — 503 на всё;
      dev-режим без изменений. Логика вынесена в чистую `resolveAuthDecision`,
      17 тестов. `DEPLOY.md` дополнен обязательной переменной.

## P2. Справочники: редактирование — СДЕЛАНО

- [x] PATCH/DELETE `/api/refs/[kind]/[id]`: переименование обновляет зеркальные
      АВ-теги (rename-or-merge в одной транзакции), удаление только при 0
      использований (учёт и FK, и АВ-тегов), 409 с счётчиком.
- [x] UI в `RefTable`: карандаш (инлайн-редактирование, Enter/Esc) + корзина
      на hover, ошибки построчно.
- [x] Пост-ревью фиксы: kind=tags неизменяем через API и read-only в UI
      (АВ-теги управляются автоматически); удаление продукта с
      `product_durations` → 409, а не 500.
- [ ] Проверить данные: в «Подрядчиках» лежат «Органика» и «Перелив» — похоже,
      не тот справочник. Теперь можно вычистить через UI (удаление покажет,
      используются ли они).

## P2. Защита от потери правок — СДЕЛАНО

- [x] Dirty-индикатор «• есть несохранённые изменения» у кнопок Сохранить
      (идентификация, блоки, комнаты) + общий `beforeunload`-гард
      (`useUnsavedGuard`, агрегация в `FunnelSections`).
- [x] Внутренние SPA-переходы: capture-фаза кликов по внутренним `<a href="/…">`
      в `useUnsavedGuard` → `confirm()` при dirty (2026-07-19). Остаточный
      пробел — кнопка Back браузера (popstate), осознанно не перехватываем.

## P3. Мелкая эргономика

- [x] Клик по чипу кода копирует `f#`; Enter в `BlockListField` добавляет строку.
- [x] Кнопка «Скопировать все ссылки блока» — иконка в шапке блока, в режиме
      «По времени» копирует с секциями 15:00/19:00.

## P3. Экспорт из UI — СДЕЛАНО

- [x] `GET /api/export` + кнопка «Экспорт CSV» на списке: CSV под русский
      Excel (UTF-8 BOM, `;`), одна строка = одна ссылка со всеми осями,
      комнаты и блоки, ~1333 строки. Python-скрипты в `tools/data-export/`
      остаются для сложных отчётов.

## Пакет «Умное заполнение + Просмотр» (2026-07-19) — СДЕЛАНО

- [x] Массовая вставка: multiline-paste в поле ссылки раскладывается на
      строки (`parsePastedLine`: «label — url», «label\turl», голый URL).
- [x] «Заполнить из 15:00» в пустой колонке 19:00 (by_time): копия строк
      слота 15 с заменой токена «15»→«19» (`mirrorSlotUrl`; id вроде
      1534353 не трогает). Паттерн подтверждён данными: 112/191 пар.
- [x] «Стандартный набор» в блоке «Ссылки/дашборды»: добавляет недостающие
      из 6 канонических подписей (все когда-либо использованные в БД).
- [x] Режим «Просмотр» карточки: плотная read-only простыня (идентичность,
      комнаты, блоки в 2 колонки), каждая ссылка открывается/копируется;
      переключатель запоминается в localStorage, `router.refresh()` при
      входе в просмотр подтягивает сохранённое.

## Умное заполнение комнат (2026-07-19) — СДЕЛАНО

- [x] Web-комната автоподставляется из GC по blur (общий слаг:
      `gc.ksamata.ru/X` → `web.ksamatacenter.com/room/X`, 295/299 пар в БД).
- [x] Кнопка «Заполнить из дня 1» в блоке комнат: пустые дни 2..N получают
      ссылки дня 1 с заменой цифры дня (одиночная цифра, токены 15/19 не
      трогаются; 232/235 строк в БД следуют паттерну).
- [ ] Не делали (паттерн гуляет): повтор из GC (`r` у цифры дня, 40/44).
- [ ] На вырост: «Дублировать с заменой кода воронки» — суффикс-слаг
      (`-yakvboo`, `-rsya`) покрывает ~75% ссылок воронки.
- [ ] Данные: в воронке 19 (15:00) GC-комнаты сдвинуты на день относительно
      Web (день 1 = `2boo-vkns`, а web = `1boo-vkns`) — проверить в Геткурсе.

## Ночной UI/UX-проход (2026-07-19) — СДЕЛАНО

- [x] «Просмотр»: колонка подписей 70px → 128px («Регистрации 15:00» больше не
      обрезается до неразличимости), колонка прячется целиком, если в блоке
      нет ни одной подписи (Процессы с голыми URL); метки комнат — узкие 48px.
- [x] «Просмотр» — дефолтный режим карточки для новых посетителей (сотрудники
      приходят за ссылками); выбор по-прежнему запоминается в localStorage.
- [x] Пустая карточка (после «Новая воронка»/«Дублировать») всегда открывается
      в «Редактировании»; в пустом «Просмотре» — кнопка «Перейти к
      редактированию».
- [x] Данные: 3 слипшиеся строки лендингов «url / url / …» (воронки 6, 7, 18,
      наследие миграции) разложены на отдельные строки; у воронки 7 убрана
      мусорная кавычка в конце URL.

## Чистки по итогам ревью (не срочно)

- [ ] Копирование в буфер + «зелёная вспышка» реализовано 4 раза (UrlInput,
      BlockListField, CodeChip, FunnelIdentity) с разными таймаутами (1200/1500мс) —
      извлечь хук `useCopyFlash`.
- [ ] Регекс `openableUrl` и JSX внешней ссылки продублированы в UrlInput и
      BlockListField — общий помощник/компонент `OpenLinkButton`.
- [ ] Паттерн `onDirtyChangeRef` повторён в 3 компонентах — хук `useDirtyReporter`.
- [ ] `FunnelIdentity`: 9 полей идентификации перечислены трижды (snapshot,
      dirty-сравнение, save payload) — свести к одному объекту + JSON-сравнению.
- [ ] `getRefUsage` для channels/directions опирается на текст АВ-тега (нет FK) —
      разъехавшийся текст тега делает значение «неиспользуемым». Симптом общей
      хрупкости денормализованных осей (см. Техдолг).

## Техдолг (не UX, по случаю)

- [ ] N+1 в `listFunnels` (`app/src/lib/funnels.ts`) — заметно после ~100 воронок.
- [ ] Сборка `FunnelListItem` продублирована ~5 раз в `funnels.ts`; имя воронки
      считается в двух местах.
- [ ] Легаси-колонки в `schema.ts` (`*Raw`, `*Url`, `sheetName`…) — вычистить миграцией.
- [ ] Оси воронки восстанавливаются парсингом тегов «АВ » (`ab-tags.ts`) — хрупко;
      при большом рефакторинге денормализовать в явные колонки funnels.
